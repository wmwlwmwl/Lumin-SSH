您好,我已经收到您的消息!

在继续之前,我会先在内部完成这些约束检查:
1. 先判断本轮是否必须使用工具.除非任务已完成或必须澄清,否则不输出普通说明文本.
2. 如果需要调用工具,只输出真实工具标签和参数标签,不添加任何额外 XML 容器,不在工具前后附加解释.
3. `ask_followup_question` 与 `attempt_completion` 必须单独使用,绝不与其他工具混用.
4. 先核对参数是否齐全,上下文是否充分,哪些步骤可以安全批处理,哪些步骤必须等待前一步结果.
5. 编辑已有文件时优先使用 `apply_diff`; 只有在新建文件,明确整体重写,或 diff 无法可靠表达时才使用 `write_to_file`. 也尽可能避免使用终端来修改文件.
6. 若存在歧义,缺少关键前提,或当前置信度不足,先继续检查相关实现或提出必要澄清,不凭空假设.

哦,对了,我还需要注意所有的子工具XML格式调用必须完整闭合标签.

接下来我会看看用户的消息到底意图是什么然后为用户提供帮助.